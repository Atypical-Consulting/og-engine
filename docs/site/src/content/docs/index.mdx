---
title: Kill Puppeteer. Generate Images in 2ms.
description: Generate OG images, social cards, and email banners in ~2ms — up to 500x faster than Puppeteer. No Chrome, no Xvfb, no cold starts. Free tier available.
template: splash
hero:
  tagline: "Server-side image generation API. Send JSON, get a PNG. No headless browser, no cold starts, no infrastructure headaches."
  actions:
    - text: Get Your API Key — Free, No Card
      link: /quick-start/
      icon: right-arrow
    - text: Try the Playground
      link: /playground/
      variant: minimal
head:
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "How is OG Engine so fast?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "OG Engine uses Pretext for text measurement — the same Unicode segmentation engine, running server-side with Canvas. No browser startup, no DOM layout, no paint cycle. Just math and pixels. Text layout in ~0.1ms, full render in ~2ms (benchmarked)."
            }
          },
          {
            "@type": "Question",
            "name": "Does OG Engine handle non-Latin scripts like CJK and Arabic?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Yes. Pretext handles CJK (Chinese, Japanese, Korean), Arabic (with bidirectional text), emoji, grapheme clusters, and mixed-script content. Pre-loaded fonts include Noto Sans JP and Noto Sans AR."
            }
          },
          {
            "@type": "Question",
            "name": "Can I validate text without generating an image?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Yes. POST /validate checks if your text fits a layout — free, unlimited, no authentication required. Use it to catch overflow before rendering."
            }
          },
          {
            "@type": "Question",
            "name": "Is there a free plan for OG Engine?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Yes. 500 renders/month, forever. No credit card, no expiration. Same engine, same speed, same quality as paid plans."
            }
          },
          {
            "@type": "Question",
            "name": "Can I self-host OG Engine?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Yes. OG Engine ships as an open-source Docker image. Run it on your own infrastructure with zero per-render cost."
            }
          },
          {
            "@type": "Question",
            "name": "What about custom templates?",
            "acceptedAnswer": {
              "@type": "Answer",
              "text": "Scale plan (€99/mo) supports custom JSON templates. All plans get 4 built-in templates. A visual template builder is on the roadmap."
            }
          }
        ]
      }
---

import PlaygroundMini from '../../components/PlaygroundMini';
import HeroTerminal from '../../components/HeroTerminal';
import HeroRender from '../../components/HeroRender';
import BenchmarkCounters from '../../components/BenchmarkCounters';
import OutputGallery from '../../components/OutputGallery';
import FaqAccordion from '../../components/FaqAccordion';
import ScrollReveal from '../../components/ScrollReveal';

<div class="hero-showcase">
  <HeroRender client:visible />
</div>

<div class="trust-bar not-content">
  <div class="trust-item">
    <a href="https://github.com/Atypical-Consulting/og-engine"><img src="https://img.shields.io/github/stars/Atypical-Consulting/og-engine?style=flat&color=38ef7d&labelColor=0c1018" alt="GitHub stars" width="80" height="20" /></a>
  </div>
  <div class="trust-separator">/</div>
  <div class="trust-item">~2ms average render</div>
  <div class="trust-separator">/</div>
  <div class="trust-item">Open source + self-hostable</div>
  <div class="trust-separator">/</div>
  <div class="trust-item">Free tier — no credit card</div>
</div>

{/* ── SECTION 1: Interactive Playground (moved up — time-to-aha-moment) ── */}

<ScrollReveal client:visible>

## Try It Now — Zero Setup

Edit the fields and watch the image rebuild instantly. No API key, no backend, no signup.

<div class="playground-mini-wrapper">
  <PlaygroundMini client:visible />
</div>

<p style="text-align:center;margin-top:1rem;">
  <a href="/playground/" class="mid-cta-btn">Open Full Playground &rarr;</a>
</p>

</ScrollReveal>

{/* ── SECTION 2: Output Quality Showcase (one large example) ── */}

<ScrollReveal client:visible>

## Production-Quality Output

Every image rendered server-side in under 3ms. Pixel-perfect text layout, gradient backgrounds, accent bars — no browser required.

<OutputGallery client:visible />

</ScrollReveal>

{/* ── SECTION 3: Benchmarks (one hero number + comparison) ── */}

## The Numbers

<BenchmarkCounters client:visible />

<div class="tech-stack-bar">
  Built with
  <a href="https://github.com/chenglou/pretext" target="_blank" rel="noopener">Pretext</a>
  <span class="tech-sep">+</span>
  <a href="https://github.com/nicknisi/canvas" target="_blank" rel="noopener">@napi-rs/canvas</a>
  <span class="tech-sep">+</span>
  <a href="https://hono.dev" target="_blank" rel="noopener">Hono</a>
</div>

{/* ── SECTION 4: How It Works ── */}

<ScrollReveal client:visible>

## How It Works

<div class="how-it-works not-content">
<div class="step">
<div class="step-number">1</div>
<div class="step-content">
<h3>Send JSON</h3>
<p>POST your title, description, format, and style to <code>/render</code>.</p>
</div>
</div>
<div class="step">
<div class="step-number">2</div>
<div class="step-content">
<h3>Engine Renders</h3>
<p>Text is measured, lines are broken, and the image is composited — all server-side, no browser.</p>
</div>
</div>
<div class="step">
<div class="step-number">3</div>
<div class="step-content">
<h3>Get Your Image</h3>
<p>PNG or WebP in the response. Typical render: ~2ms. Supports CJK, Arabic, emoji, and mixed scripts.</p>
</div>
</div>
</div>

</ScrollReveal>

{/* ── SECTION 5: Use Cases ── */}

<ScrollReveal client:visible>

## Use Cases

<div class="use-cases not-content">
<div class="use-case">
<div class="use-case-icon"><span>{"{ }"}</span></div>
<h3>Blog Platforms</h3>
<p>Auto-generate unique OG images for every post. No design tool, no template editor — just your title and brand colors.</p>
</div>
<div class="use-case">
<div class="use-case-icon"><span>{"~>"}</span></div>
<h3>SaaS Products</h3>
<p>Dynamic social cards when users share dashboards, reports, or public pages. Render on demand, cache at the edge.</p>
</div>
<div class="use-case">
<div class="use-case-icon"><span>$_</span></div>
<h3>E-Commerce</h3>
<p>Product images with price overlays, sale badges, and localized text — generated at scale for every SKU.</p>
</div>
<div class="use-case">
<div class="use-case-icon"><span>@:</span></div>
<h3>Email Marketing</h3>
<p>Personalized banners with recipient name, offer details, or dynamic content. No design bottleneck.</p>
</div>
</div>

</ScrollReveal>

{/* ── SECTION 6: Free Text Validation ── */}

<ScrollReveal client:visible>

## Free Text Validation

Check if your text fits — **free, unlimited, no signup required.**

<div class="validate-terminal">
  <div class="validate-terminal-bar">
    <div class="hero-terminal-dots">
      <span class="dot dot-red"></span>
      <span class="dot dot-yellow"></span>
      <span class="dot dot-green"></span>
    </div>
    <span class="hero-terminal-title">validate — text-fit check</span>
  </div>
  <pre class="validate-terminal-body"><code>{"$ curl -X POST https://og-engine.com/validate \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"format\":\"og\",\"title\":\"Will this headline fit?\"}'\n\n"}<span class="term-response">{"HTTP/1.1 200 OK"}</span>{"\n"}<span class="term-header-accent">{"X-Compute-Time-Ms: 0.08"}</span>{"\n\n{\n  "}<span style={{color:'#67e8f9'}}>{'"fits"'}</span>{": "}<span style={{color:'#38ef7d'}}>{"true"}</span>{",\n  "}<span style={{color:'#67e8f9'}}>{'"title"'}</span>{": { "}<span style={{color:'#67e8f9'}}>{'"lines"'}</span>{": "}<span style={{color:'#fbbf24'}}>{"1"}</span>{", "}<span style={{color:'#67e8f9'}}>{'"maxLines"'}</span>{": "}<span style={{color:'#fbbf24'}}>{"3"}</span>{", "}<span style={{color:'#67e8f9'}}>{'"overflow"'}</span>{": "}<span style={{color:'#38ef7d'}}>{"false"}</span>{" }\n}"}</code></pre>
</div>

<div class="validate-callout">

`POST /validate` is always free. No API key. No rate limits. Use it in your CI pipeline, your CMS, your form validation — anywhere you need to know if text fits before rendering.

[Try it in the Playground &rarr;](/playground/)

</div>

</ScrollReveal>

{/* ── SECTION 7: FAQ (critical objection-handlers BEFORE pricing) ── */}

<ScrollReveal client:visible>

## Common Questions

<FaqAccordion client:visible />

</ScrollReveal>

{/* ── SECTION 8: Pricing (with Pro as the featured plan — DECISIONS.md #7) ── */}

<ScrollReveal client:visible>

## Pricing

All plans use the same engine — same speed, same quality, same fonts. Pick the quota that fits.

<div class="pricing-strip not-content">
<a href="/quick-start/" class="pricing-strip-card pricing-strip-muted">
<div class="pricing-strip-tier">Free</div>
<div class="pricing-strip-price"><span class="currency">&euro;</span>0<span class="period">/mo</span></div>
<div class="pricing-strip-quota">500 renders</div>
</a>
<a href="https://buy.stripe.com/starter" class="pricing-strip-card">
<div class="pricing-strip-tier">Starter</div>
<div class="pricing-strip-price"><span class="currency">&euro;</span>10<span class="period">/mo</span></div>
<div class="pricing-strip-quota">10k renders</div>
</a>
<a href="https://buy.stripe.com/pro" class="pricing-strip-card pricing-strip-featured">
<div class="pricing-strip-badge">Most Popular</div>
<div class="pricing-strip-tier">Pro</div>
<div class="pricing-strip-price"><span class="currency">&euro;</span>39<span class="period">/mo</span></div>
<div class="pricing-strip-quota">50k renders</div>
</a>
<a href="mailto:sales@og-engine.com" class="pricing-strip-card pricing-strip-muted">
<div class="pricing-strip-tier">Scale</div>
<div class="pricing-strip-price"><span class="currency">&euro;</span>99<span class="period">/mo</span></div>
<div class="pricing-strip-quota">200k renders</div>
</a>
</div>

<p class="pricing-strip-note">5 formats (OG, Twitter, Square, LinkedIn, Story) &middot; 4 templates &middot; 8 fonts &middot; <a href="/pricing/">Full comparison &rarr;</a></p>

</ScrollReveal>

{/* ── SECTION 9: Open Source ── */}

<ScrollReveal client:visible>
<div class="oss-callout">

## Open Source

OG Engine is open source. Run it on your own infrastructure with **zero per-render cost**, or use the hosted API and let us handle scaling, caching, and uptime.

<div class="oss-actions">
  <a href="https://github.com/Atypical-Consulting/og-engine" class="oss-link">View on GitHub &rarr;</a>
  <a href="/self-hosting/docker/" class="oss-link">Self-Hosting Guide &rarr;</a>
</div>

</div>
</ScrollReveal>

{/* ── SECTION 10: Closing CTA (for the scrolled-to-bottom visitor) ── */}

<div class="closing-cta not-content">
  <div class="closing-cta-inner">
    <h2 class="closing-cta-title">Ready to kill Puppeteer?</h2>
    <p class="closing-cta-desc">Start generating images in under 2ms. Free forever, no credit card.</p>
    <a href="/quick-start/" class="closing-cta-btn">Get Your API Key &mdash; It's Free</a>
  </div>
</div>
